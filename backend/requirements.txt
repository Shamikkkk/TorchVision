fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-chess>=1.999
stockfish>=3.28.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# ── model_training dependencies (also needed at runtime for neural mode) ──────
# Install PyTorch separately. The dev venv runs the CUDA wheel (2.6.0+cu124) and
# the RTX 3050's CUDA works (verified July 2026):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#
# CPU-only fallback (much smaller download):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch

# Data pipeline (only needed when running model_training scripts)
zstandard>=0.22.0
requests>=2.32.0
